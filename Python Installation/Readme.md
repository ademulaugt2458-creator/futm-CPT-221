For hasle free, Run the "run_all.bat" file to install python and all required packages.
Make sure to run the file as administrator.
Ensure the remaining files are in the same directory as the "run_all.bat" file.
And lastly, make sure to have an active internet connection.